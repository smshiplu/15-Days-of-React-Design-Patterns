# Day 02 - Your Tasks

Build a simple Contact Form

- Implement as a controlled component
- Implement as an uncontrolled component(With and Without Ref)

Create a Comparison Table:

- Compare pros/cons: code styles, validation, reset, readability, complexity.
- Create an MD file and capture the findings.

Create a comparison table in markdown of controlled vs uncontrolled with ref vs uncontrolled without ref React components in terms of pros and cons with the aspects of code styles, validation, reset, readability, and complexity.
